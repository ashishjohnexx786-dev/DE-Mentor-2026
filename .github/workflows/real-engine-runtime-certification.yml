name: M09-M14 Real Engine Runtime Certification 2026
on:
  push:
    branches: [main, runtime-candidate/real-engine-repair2-2026-09-12]
permissions:
  contents: read

jobs:
  product-integrity:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Verify staged Runtime Repair 2 bytes and root manifest
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          import hashlib,zipfile,json
          sha=lambda p: hashlib.sha256(Path(p).read_bytes()).hexdigest()
          m=Path('course_packages/modules/M13_DELTA_LAKEHOUSE_CANONICAL_MODULE_FREEZE_QA_PASS.zip')
          expected='e878b22a5182b28406de4aa89320551ff0512ff9f140d90f9b088bc30296f2ad'
          assert sha(m)==expected,(sha(m),expected)
          with zipfile.ZipFile(m) as z:
              assert z.testzip() is None
              script=z.read('M13/04_LAB_ASSETS/delta_reference/delta_reference_lab.py').decode()
              assert "spark.sql.extensions','io.delta.sql.DeltaSparkSessionExtension" in script
              assert "spark.sql.catalog.spark_catalog','org.apache.spark.sql.delta.catalog.DeltaCatalog" in script
          for line in Path('MANIFEST_SHA256.txt').read_text().splitlines():
              if not line.strip(): continue
              h,rel=line.split('  ',1)
              assert Path(rel).is_file(),rel
              assert sha(rel)==h,(rel,sha(rel),h)
          receipt=json.loads(Path('control/SDE_RC5_RUNTIME_REPAIR2_REAL_ENGINE_DISCOVERY_2026-09-12.json').read_text())
          assert receipt['repair']['successor_sha256']==expected
          assert receipt['final'] is False
          assert "runtime-repair2-v4" in Path('sw.js').read_text()
          print('RUNTIME REPAIR2 PRODUCT INTEGRITY PASS',expected)
          PY

  m09-dbt:
    needs: product-integrity
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Extract M09
        run: |
          python - <<'PY'
          from pathlib import Path
          import zipfile
          hits=[p for p in Path('course_packages').rglob('*.zip') if 'M09' in p.name.upper() and 'G0' not in p.name.upper()]
          assert hits
          with zipfile.ZipFile(sorted(hits)[0]) as z: assert z.testzip() is None; z.extractall('/tmp/m09')
          PY
      - name: Run canonical M09 pipeline controls
        shell: bash
        run: |
          set -euo pipefail
          PIPE=$(find /tmp/m09 -name m09_pipeline.py -print -quit)
          test -n "$PIPE"
          python "$PIPE" | tee /tmp/m09-pipeline.log
          SMOKE=$(find /tmp/m09 -name M09_SMOKE_RESULT.json -print -quit)
          python - "$SMOKE" <<'PY'
          import json,sys
          x=json.load(open(sys.argv[1]))
          assert x['baseline']['status']=='PASS'
          assert x['baseline']['quarantine']==3
          assert x['incremental']['status']=='PASS'
          assert x['rerun']['selected']==0
          assert x['failure']['status']=='FAILED_VALIDATION'
          assert x['failure_state_unchanged'] is True
          assert all(x['tests'].values())
          assert x['quarantine_total']==3
          assert x['audit_runs']==3
          print('M09 CANONICAL PIPELINE CONTROLS PASS')
          PY
      - name: Run dbt extension against canonical accepted current rows
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install -q 'dbt-duckdb>=1.9,<2' duckdb
          PROJECT=$(dirname "$(find /tmp/m09 -name dbt_project.yml -print -quit)")
          SQLITE=$(find /tmp/m09 -name retailpulse.db -print -quit)
          test -n "$PROJECT" && test -n "$SQLITE"
          python - "$SQLITE" <<'PY'
          import sys,sqlite3,duckdb
          src=sqlite3.connect(sys.argv[1])
          rows=src.execute('select order_id,customer_id,order_date,region,status,qty,unit_price,updated_at from stg_orders order by order_id').fetchall()
          src.close(); assert len(rows)==12,len(rows)
          con=duckdb.connect('/tmp/m09.duckdb')
          con.execute('create schema if not exists main')
          con.execute('create or replace table main.raw_orders(order_id varchar, customer_id varchar, order_date varchar, region varchar, status varchar, qty integer, unit_price double, updated_at varchar)')
          con.executemany('insert into main.raw_orders values (?,?,?,?,?,?,?,?)',rows)
          con.close()
          PY
          mkdir -p /tmp/dbt_profiles
          cat >/tmp/dbt_profiles/profiles.yml <<'YML'
          m09_local:
            target: ci
            outputs:
              ci:
                type: duckdb
                path: /tmp/m09.duckdb
                schema: main
                threads: 2
          YML
          dbt debug --project-dir "$PROJECT" --profiles-dir /tmp/dbt_profiles
          dbt parse --project-dir "$PROJECT" --profiles-dir /tmp/dbt_profiles
          dbt run --project-dir "$PROJECT" --profiles-dir /tmp/dbt_profiles
          dbt test --project-dir "$PROJECT" --profiles-dir /tmp/dbt_profiles
          dbt run --project-dir "$PROJECT" --profiles-dir /tmp/dbt_profiles
          dbt test --project-dir "$PROJECT" --profiles-dir /tmp/dbt_profiles
          python - <<'PY'
          import duckdb
          con=duckdb.connect('/tmp/m09.duckdb')
          rows=con.execute('select count(*) from main.fct_sales').fetchone()[0]
          uniq=con.execute('select count(distinct order_id) from main.fct_sales').fetchone()[0]
          nulls=con.execute("select count(*) from main.fct_sales where order_id is null or customer_id is null or trim(customer_id)='' ").fetchone()[0]
          revenue=con.execute('select round(sum(line_revenue),2) from main.fct_sales').fetchone()[0]
          print('FCT_ROWS',rows,'UNIQUE_IDS',uniq,'NULL_KEY_ROWS',nulls,'REVENUE',revenue)
          assert rows==11 and rows==uniq and nulls==0 and float(revenue)==3490.0
          PY
          echo 'M09 PIPELINE + DBT REAL ENGINE PASS'

  m10-docker:
    needs: product-integrity
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Extract M10
        run: |
          python - <<'PY'
          from pathlib import Path
          import zipfile
          hits=[p for p in Path('course_packages').rglob('*.zip') if 'M10' in p.name.upper() and 'G0' not in p.name.upper()]
          assert hits
          with zipfile.ZipFile(sorted(hits)[0]) as z: assert z.testzip() is None; z.extractall('/tmp/m10')
          PY
      - name: Build and run canonical Docker Compose lab
        shell: bash
        run: |
          set -euo pipefail
          D=$(dirname "$(find /tmp/m10 -path '*/docker/compose.yaml' -print -quit)")
          test -n "$D"; cd "$D"
          docker compose config >/tmp/m10-compose-rendered.yml
          docker compose build
          docker compose up -d
          trap 'docker compose logs --no-color || true; docker compose down -v || true' EXIT
          for i in $(seq 1 60); do if curl -fsS http://127.0.0.1:8080/ >/tmp/m10-response.json; then break; fi; sleep 2; done
          python - <<'PY'
          import json
          x=json.load(open('/tmp/m10-response.json')); print(x)
          assert x=={'service':'m10-lab','status':'ok','db_host':'db'},x
          PY
          echo 'M10 DOCKER COMPOSE REAL ENGINE PASS'

  m10-airflow:
    needs: product-integrity
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      AIRFLOW_HOME: /tmp/airflow
      AIRFLOW__CORE__DAGS_FOLDER: /tmp/airflow/dags
    steps:
      - uses: actions/checkout@v4
      - name: Extract M10 DAG
        run: |
          python - <<'PY'
          from pathlib import Path
          import zipfile
          hits=[p for p in Path('course_packages').rglob('*.zip') if 'M10' in p.name.upper() and 'G0' not in p.name.upper()]
          assert hits
          with zipfile.ZipFile(sorted(hits)[0]) as z: assert z.testzip() is None; z.extractall('/tmp/m10')
          PY
      - name: Parse canonical DAG in exact Airflow 3.3.1 runtime
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install -q 'apache-airflow==3.3.1'
          DAG=$(find /tmp/m10 -name m10_daily_orders.py -print -quit)
          test -n "$DAG"; mkdir -p "$AIRFLOW_HOME/dags"; cp "$DAG" "$AIRFLOW_HOME/dags/"
          airflow version
          airflow db migrate
          python - <<'PY'
          from airflow.dag_processing.dagbag import DagBag
          bag=DagBag(dag_folder='/tmp/airflow/dags')
          print('IMPORT_ERRORS',bag.import_errors,'DAGS',sorted(bag.dags))
          assert not bag.import_errors,bag.import_errors
          dag=bag.get_dag('m10_daily_orders'); assert dag is not None
          ids=set(dag.task_ids); print('TASKS',sorted(ids)); assert ids=={'extract','validate','transform','load','reconcile'}
          assert set(dag.task_dict['extract'].downstream_task_ids)=={'validate'}
          assert set(dag.task_dict['validate'].downstream_task_ids)=={'transform'}
          assert set(dag.task_dict['transform'].downstream_task_ids)=={'load'}
          assert set(dag.task_dict['load'].downstream_task_ids)=={'reconcile'}
          assert dag.task_dict['extract'].retries==2
          assert dag.catchup is False
          PY
          echo 'M10 AIRFLOW 3.3.1 REAL ENGINE DAG PARSE PASS'

  m12-spark:
    needs: product-integrity
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Extract M12
        run: |
          python - <<'PY'
          from pathlib import Path
          import zipfile
          hits=[p for p in Path('course_packages').rglob('*.zip') if 'M12' in p.name.upper() and 'G0' not in p.name.upper()]
          assert hits
          with zipfile.ZipFile(sorted(hits)[0]) as z: assert z.testzip() is None; z.extractall('/tmp/m12')
          PY
      - name: Run canonical PySpark 4.2 pipeline and tests
        shell: bash
        run: |
          set -euo pipefail
          REF=$(find /tmp/m12 -name m12_reference_pipeline.py -print -quit)
          TEST=$(find /tmp/m12 -name test_transformations.py -print -quit)
          REQ=$(find /tmp/m12 -path '*/pyspark_reference/requirements.txt' -print -quit)
          test -n "$REF" && test -n "$TEST" && test -n "$REQ"
          python -m pip install -q -r "$REQ"
          java -version
          python "$REF" | tee /tmp/m12-reference.log
          python "$TEST"
          grep -q 'orders 16 joined 16' /tmp/m12-reference.log
          echo 'M12 PYSPARK REAL ENGINE PASS'

  m13-delta:
    needs: product-integrity
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - name: Extract exact packaged Runtime Repair 2 M13
        run: |
          python - <<'PY'
          from pathlib import Path
          import zipfile,hashlib
          p=Path('course_packages/modules/M13_DELTA_LAKEHOUSE_CANONICAL_MODULE_FREEZE_QA_PASS.zip')
          h=hashlib.sha256(p.read_bytes()).hexdigest()
          assert h=='e878b22a5182b28406de4aa89320551ff0512ff9f140d90f9b088bc30296f2ad',h
          with zipfile.ZipFile(p) as z: assert z.testzip() is None; z.extractall('/tmp/m13')
          PY
      - name: Run packaged Delta reference directly with no patching
        shell: bash
        run: |
          set -euo pipefail
          REF=$(find /tmp/m13 -name delta_reference_lab.py -print -quit)
          REQ=$(find /tmp/m13 -path '*/delta_reference/requirements.txt' -print -quit)
          test -n "$REF" && test -n "$REQ"
          grep -q "DeltaSparkSessionExtension" "$REF"
          grep -q "DeltaCatalog" "$REF"
          python -m pip install -q -r "$REQ"
          java -version
          python "$REF" | tee /tmp/m13-delta.log
          grep -q 'CURRENT' /tmp/m13-delta.log
          grep -q 'HISTORY' /tmp/m13-delta.log
          grep -q 'VERSION0' /tmp/m13-delta.log
          echo 'M13 PACKAGED RUNTIME REPAIR2 DELTA REAL ENGINE PASS'

  m14-kafka:
    needs: product-integrity
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Extract M14
        run: |
          python - <<'PY'
          from pathlib import Path
          import zipfile
          hits=[p for p in Path('course_packages').rglob('*.zip') if 'M14' in p.name.upper() and 'G0' not in p.name.upper()]
          assert hits
          with zipfile.ZipFile(sorted(hits)[0]) as z: assert z.testzip() is None; z.extractall('/tmp/m14')
          PY
      - name: Start real Kafka broker and run canonical producer consumer
        shell: bash
        run: |
          set -euo pipefail
          D=$(dirname "$(find /tmp/m14 -path '*/kafka_reference/compose.yaml' -print -quit)")
          test -n "$D"; cd "$D"
          python -m pip install -q -r requirements.txt
          docker compose config >/tmp/m14-compose-rendered.yml
          docker compose up -d
          trap 'docker compose logs --no-color || true; docker compose down -v || true' EXIT
          for i in $(seq 1 60); do if docker exec m14-kafka /opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server localhost:9092 >/dev/null 2>&1; then break; fi; sleep 2; done
          docker exec m14-kafka /opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:9092 --create --if-not-exists --topic m14-events --partitions 2 --replication-factor 1
          python producer.py
          timeout 45s python consumer.py | tee /tmp/m14-consumer.log
          test "$(grep -c 'm14-events' /tmp/m14-consumer.log)" -eq 5
          docker exec m14-kafka /opt/kafka/bin/kafka-consumer-groups.sh --bootstrap-server localhost:9092 --group m14-lab --describe | tee /tmp/m14-group.txt
          grep -q 'm14-events' /tmp/m14-group.txt
          echo 'M14 KAFKA REAL ENGINE PASS'
