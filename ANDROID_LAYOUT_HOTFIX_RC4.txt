DE MENTOR — ANDROID LAYOUT HOTFIX RC4

Observed on real Android Chrome at reported 452px viewport:
PASS / GOOD:
- progress storage
- Stage 00 book availability
- service worker / PWA support
- mobile screen detection
- current connection

LIMITED / EXPECTED:
- timer notification permission was not granted by the browser; real-clock timer remains usable.

FAIL FOUND:
- lesson classroom steps 3–8 could overlap/collapse on the real Android layout.

RC4 FIX:
- lock browser text-size adjustment to 100%
- give teacher-step content an explicit shrinkable min-width
- switch teacher steps to a one-column mobile layout at <=600px
- stack lesson action buttons on phones
- bump service-worker cache key so old CSS is replaced

RETEST REQUIRED:
1. Refresh/reopen the deployed RC4 site.
2. Open Stage 00 / first lesson.
3. Scroll through steps 1–8.
4. PASS only if every step is separate, full-width and readable with no overlap.
5. Re-run the 1-minute lock-screen timer test separately.
